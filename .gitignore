# Env / secrets
.env
.env.*
!.env.example

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Vector store (regenerated on each run; for `chroma run --path ./chroma_db`)
chroma_db/

# Local trace log (regenerated on each query)
traces.jsonl

# Uploaded user files (do not commit)
data/uploads/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
